apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: demo-icm-ssr
  namespace: demo
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: demo-icm-as
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 300
