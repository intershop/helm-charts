apiVersion: v1
kind: Service
metadata:
  name: {{ include "pwa.componentName" (dict "root" . "component" "proxy") }}
  labels:
    {{- include "pwa.componentLabels" (dict "root" . "component" "proxy") | nindent 4 }}
spec:
  type: {{ .Values.proxy.service.type }}
  selector:
    {{- include "pwa.selectorLabels" (dict "root" . "component" "proxy") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.proxy.service.port }}
      targetPort: http
      protocol: TCP
---
{{- if include "pwa.proxyMetricsEnabled" . }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "pwa.proxyMetricsFullname" . }}
  labels:
    {{- include "pwa.componentLabels" (dict "root" . "component" "proxy") | nindent 4 }}
  annotations:
    prometheus.io/scrape: 'true'
    prometheus.io/port: {{ .Values.proxy.ports.metrics | quote }}
spec:
  clusterIP: None
  ports:
    - name: metrics
      port: {{ .Values.proxy.ports.metrics }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "pwa.selectorLabels" (dict "root" . "component" "proxy") | nindent 4 }}
{{- end -}}
