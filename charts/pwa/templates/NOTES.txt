Intershop PWA has been deployed.

Chart:    {{ .Chart.Name }}-{{ .Chart.Version }}
Release:  {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

🚀 Deployment Components:
  - app   : {{ include "pwa.componentName" (dict "root" . "component" "app") }} ({{ .Values.app.replicaCount }} replicas, Angular SSR)
  - proxy : {{ include "pwa.componentName" (dict "root" . "component" "proxy") }} ({{ .Values.proxy.replicaCount }} replicas, nginx/OpenResty)

⚙️  Configuration:
  - ICM Base URL: {{ .Values.config.icmBaseUrl }}
{{- if .Values.config.icmBaseUrlSsr }}
  - ICM SSR URL: {{ .Values.config.icmBaseUrlSsr }}
{{- end }}
{{- if .Values.config.allowedHosts }}
  - Allowed Hosts: {{ .Values.config.allowedHosts }}
{{- end }}

🔄 Proxy forwards traffic to app via:
  {{ include "pwa.appUpstream" . }}

{{- if .Values.ingress.enabled }}

🌐 Access the PWA through the configured Ingress host(s):
{{- if .Values.ingress.instances }}
{{- range $key, $instance := .Values.ingress.instances }}
{{- if $instance.hosts }}
  Instance: {{ $key }}
{{- range $instance.hosts }}
  http{{ if $instance.tlsSecretName }}s{{ end }}://{{ .host }}{{ if .path }}{{ .path }}{{ end }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
{{- else }}

💻 Access the PWA locally using port-forward:

  kubectl --namespace {{ .Release.Namespace }} port-forward \
    svc/{{ include "pwa.componentName" (dict "root" . "component" "proxy") }} 8080:{{ .Values.proxy.service.port }}

  Then open: http://localhost:8080
{{- end }}

📚 For more information and troubleshooting, visit:
  https://github.com/intershop/helm-charts/tree/main/charts/pwa
  https://github.com/intershop/intershop-pwa
