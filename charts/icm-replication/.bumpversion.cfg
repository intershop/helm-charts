[bumpversion]
current_version = 0.0.1
commit = true
tag = false
message = Bump icm-replication chart version: {current_version} → {new_version}

[bumpversion:file:Chart.yaml]
search = version: {current_version}
replace = version: {new_version}
