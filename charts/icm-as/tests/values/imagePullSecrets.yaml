# used for test:
# image_test.yaml/custom pull secrets
#
# the following does not work as expected:
# ----------------------------------------
#tests:
#  - it: custom pull secrets
#    set:
#      imagePullSecrets[1]: "dockerhub"
#      imagePullSecrets[0]: "custom-acr"

imagePullSecrets:
- "dockerhub"
- "custom-acr"
