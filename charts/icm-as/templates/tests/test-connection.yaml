apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "icm-as.fullname" . }}-test-connection"
  labels:
    {{- include "icm-as.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['http://{{ .Release.Name }}-icm-web-wa:8080/INTERSHOP/wastatistics']
  restartPolicy: OnFailure
