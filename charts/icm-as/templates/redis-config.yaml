{{- if .Values.webLayer.redis.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name:  {{ include "icm-as.fullname" . }}-redis-config
data:
  redis-config: |
    maxmemory-policy volatile-lru
{{- end }}
