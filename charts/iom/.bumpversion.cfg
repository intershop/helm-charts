[bumpversion]
current_version = 2.1.0
commit = true
tag = false
message = Bump IOM chart version: {current_version} → {new_version}

[bumpversion:file:Chart.yaml]
search = version: {current_version}
replace = version: {new_version}
