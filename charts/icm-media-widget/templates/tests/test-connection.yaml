apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "icm-media-widget.fullname" . }}-test-connection"
  labels:
    {{- include "icm-media-widget.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "icm-media-widget.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
