apiVersion: v1
kind: Service
metadata:
  name: {{ include "icm-media-widget.fullname" . }}
  labels:
    {{- include "icm-media-widget.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "icm-media-widget.selectorLabels" . | nindent 4 }}
