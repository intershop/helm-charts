PRODUCTION CUTOVER BLOCKED. See README.md and docs/CUTOVER.md.
Public images: https://{{ .Values.ingress.public.host }}/mediawidget/public/tenants/{tenantId}/images/{path}
Management: https://{{ .Values.ingress.management.host }}/mediawidget (shop backend credentials required)
Company portal: https://{{ .Values.ingress.control.host }} (application OIDC required)
No Azure resources, secrets, or live data migration are managed by this chart.
The Azure workload identity webhook is an external prerequisite.
{{- if or .Values.scaling.enabled .Values.httpScaling.widget.enabled .Values.httpScaling.rest.enabled }}
KEDA is an external prerequisite; this chart does not install it.
{{- end }}
{{- if or .Values.httpScaling.widget.enabled .Values.httpScaling.rest.enabled }}
HTTP scaling requires pre-installed HTTP add-on 0.11.1 / KEDA 2.18 and reviewed interceptor networking.
Verify ExternalName/Host forwarding and live activation; see README.md.
{{- if .Values.httpScaling.widget.enabled }}
The HTTP-intercepted widget remains a stateful singleton, not an elastic UI pool.
{{- end }}
{{- end }}
