apiVersion: v2
name: icm-media-widget
description: Tenant-aware media data plane and separately identified control plane
type: application
version: 0.2.0
appVersion: "2.0.0"
kubeVersion: ">=1.25.0-0"
# imgproxy is rendered locally: one chart owner, no network-dependent subchart fetch.
