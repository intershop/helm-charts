name: "Unit test icm chart"
on:
  pull_request:
    paths:
    - 'charts/icm/**'

jobs:
  lint-test-icm-unittest:
    runs-on: ubuntu-latest
    steps:
    - name: Base
      uses: "../template/base"
    - name: Unit test
      uses: "../template/unittest"
      with:
        chartTestingFile: ct_icm.yaml
        chartName: icm
